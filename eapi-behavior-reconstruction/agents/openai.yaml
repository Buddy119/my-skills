interface:
  display_name: "EAPI Repository Knowledge Reconstruction"
  short_description: "以版本化事务分析EAPI仓库并生成Tech与独立BA知识包"
  default_prompt: "Use $eapi-behavior-reconstruction --repo \"/absolute/repository/path\" --output \"/absolute/output/path\" to analyze the complete repository through the versioned stage executor. On resume, create and execute a separate migration plan before repository synthesis or Tech/API/BA publication."
